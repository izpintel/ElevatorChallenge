{
  "name": "elevator-challenge",
  "version": "1.0.0",
  "description": "Elevator simulation project using TypeScript and OOP",
  "scripts": {
    "copy-assets": "cp -r public/* dist/",
    "build": "tsc && npm run copy-assets",
    "start": "npm run build && npx serve ./dist",
    "watch": "tsc --watch",
    "clean": "rm -rf ./dist/*"
  }
}
