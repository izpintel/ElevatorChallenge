export const ELEVATOR_SPEED = 500; // milliseconds per floor
export const STOP_TIME = 2000; // milliseconds to stop at a floor
export const FLOOR_HEIGHT = 110; // height of each floor in pixels
export const DEFAULT_FLOORS_NUMBER = 10;
export const DEFAULT_ELEVATORS_NUMBER = 2;
